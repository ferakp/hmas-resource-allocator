import * as rDatabaseApi from "../../../relational-database-api/api";
import * as utils from "../utils/utils";
import * as errorMessages from "../messages/errors";
import * as actions from "../messages/actions";
import * as requestConstraints from "../request-constraints/messages";
import * as usersResponseGenerator from "../response-generators/messages";

/**
 * GET /messages and /messages/:id
 */
export async function getMessages(req, res) {}

/**
 * POST /messages/:id
 */
export async function postMessage(req, res) {}


/**
 * DELETE /messages/:id
 */
export async function deleteMessage(req, res) {}
