import * as utils from "../../utils/utils";
import * as errorMessages from "../../messages/errors";
import * as responseGenerators from "../../response-generators/nli";

export async function getNli(req, res, next) {
  // Pass
  next();
}

export async function postNli(req, res, next) {
  // Pass
  next();
}

export async function deleteNli(req, res, next) {
  // Pass
  next();
}

export async function patchNli(req, res, next) {
  // Pass
  next();
}
