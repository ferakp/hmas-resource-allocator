import * as rDatabaseApi from "../../../relational-database-api/api";
import * as utils from "../utils/utils";
import * as errorMessages from "../messages/errors";
import * as actions from "../messages/actions";
import * as requestConstraints from "../request-constraints/nli";
import * as usersResponseGenerator from "../response-generators/nli";

/**
 * GET /nli and /nli/:id
 */

export async function getNli(req, res) {}

/**
 * POST /nli/:id
 */

export async function postNli(req, res) {}