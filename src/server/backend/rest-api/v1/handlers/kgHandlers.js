import * as rDatabaseApi from "../../../relational-database-api/api";
import * as utils from "../utils/utils";
import * as errorMessages from "../messages/errors";
import * as actions from "../messages/actions";
import * as requestConstraints from "../request-constraints/kg";
import * as usersResponseGenerator from "../response-generators/kg";

/**
 * GET /kg and /kg/:id
 */
export async function getKg(req, res) {}

/**
 * POST /kg/:id
 */
export async function postKg(req, res) {}

/**
 * PATCH /kg/:id
 */
export async function patchKg(req, res) {}

/**
 * DELETE /kg/:id
 */
export async function deleteKg(req, res) {}
