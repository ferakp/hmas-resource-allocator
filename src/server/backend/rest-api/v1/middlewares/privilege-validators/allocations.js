import * as utils from "../../utils/utils";
import * as errorMessages from "../../messages/errors";
import * as responseGenerators from "../../response-generators/allocations";

export async function getAllocations(req, res, next) {
  // Pass
  next();
}

export async function postAllocation(req, res, next) {
  // Pass
  next();
}

export async function deleteAllocation(req, res, next) {
  // Pass
  next();
}

export async function patchAllocation(req, res, next) {
  // Pass
  next();
}

export async function postAllocationCompleteRequests(req, res, next) {
  // Pass
  next();
}
