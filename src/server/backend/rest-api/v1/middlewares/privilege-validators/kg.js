import * as utils from "../../utils/utils";
import * as errorMessages from "../../messages/errors";
import * as responseGenerators from "../../response-generators/kg";

export async function getKg(req, res, next) {
  // Pass
  next();
}

export async function postKg(req, res, next) {
  // Pass
  next();
}

export async function deleteKg(req, res, next) {
  // Pass
  next();
}

export async function patchKg(req, res, next) {
  // Pass
  next();
}
